import 'package:flutter/widgets.dart';

class GeneralBalanceScreen extends StatelessWidget {
  const GeneralBalanceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}